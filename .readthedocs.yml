version: 2

sphinx:
    configuration: docs/conf.py

build:
   image: latest

python:
   version: 3.7
   system_packages: true
   install:
     - requirements: docs/requirements.txt
     - method: setuptools
       path: .

formats: htmlzip
